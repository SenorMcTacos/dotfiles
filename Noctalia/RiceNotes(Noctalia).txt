Symlink Creation
command:
ln -s ~/Things/dotfiles/Noctalia/folder_name ~/.config/folder_name

Steam Dual Boot File Fix
https://github.com/ValveSoftware/Proton/wiki/Using-a-NTFS-disk-with-Linux-and-Windows
Scroll to the bottom where it mention NTFS Read Errors
Need to create a symlink of compatdata so that games that have : in it don't break

Mouse fix for steam controller
https://github.com/Supreeeme/extest
Download zip, run with rust command
Add user to input group
Change steam .desktop to execute with the LD preload
Also, need to go to steam big picture mode, go to Display > Disable Gpu Blocklist under advanced

Enable Tailscale
Download ts
sudo systemctl enable --now tailscaled
sudo tailscale up
If needed for moonlight, grab IP from admin console on tailscale and enter manually

Allowing Thunderbolt devices
Users who just want to connect any device without any sort of manual work can create a udev rule as in 99-removable.rules:

/etc/udev/rules.d/99-removable.rules

ACTION=="add", SUBSYSTEM=="thunderbolt", ATTR{authorized}=="0", ATTR{authorized}="1"

Use Nvim to edit sudo files
Set Neovim as your default editor in your shell configuration file (like ~/.bashrc or ~/.zshrc):bashexport EDITOR="nvim"
export SUDO_EDITOR="nvim"
